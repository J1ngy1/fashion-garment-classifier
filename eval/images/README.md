# Evaluation Image Folder

Place the real evaluation image files here when available.

- Recommended size: 50-100 garment/street-fashion images
- File names should match entries in `eval/labeled-test-set.json`
- Current repository includes a 60-sample labeled scaffold in JSON so evaluation can run locally without external APIs
